# Brief G4: verifier (GO or NO-GO)

You are the verifier of the mastermind team. Run the G4 walk in `PLAN-final.md` section 5.5 for the OptimalX Salla React theme in `C:\Users\Ahmed\OneDrive\Desktop\optimalx` (scratchpad C:\Users\Ahmed\AppData\Local\Temp\claude\c--Users-Ahmed-OneDrive-Desktop-optimalx\ff691bcb-344e-4a12-98cc-98d989eacf7d\scratchpad\). Commands: `pnpm typecheck` (errors that name only route ids in `app/routeTree.gen.ts` are the documented stale-process defect; list them, do not fail on them alone), `pnpm test`, `pnpm check:all`, `pnpm build`, `pnpm preview --port 5330`. Browser walk with the playwright MCP (Arabic, 390x844 then 1440x900, RTL): `/ar/`, PDPs p1996831868, p1497955814, p1141798217, p702549495, p662137586, p487045117, `/ar/latest-products`, `/ar/search?q=واي`, `/ar/search?q=zzzz`, `/ar/cart` empty and after one add to cart, `/ar/services`, `/ar/branch`, `/ar/about`, `/ar/contact`, `/ar/tools/converter`, `/ar/blog`, `/ar/brands`, `/ar/account/wishlist` logged out, `/ar/no-such-page`, `/ar/kitchen-sink`. On each: theme-attributable console errors 0 (the engine's React #418 is excluded), exactly one h1, `html[lang][dir]` correct, no horizontal scroll at 390 and 320, screenshots saved to `scratchpad/g4/<route>-<width>.png`, and on home, PDP and listing the render-budget probe (`filterMB 0, nBackdrop 0, forced 0, fixedOver 0, docW <= vw`). Report per route in `scratchpad/G4-report.md` with a final GO or NO-GO and the failing items named; do not modify source files. Return the delegation JSON envelope.
