# Brief G2: UI and design review (designer gate)

You are the creative direction reviewer of the mastermind team (gate G2). Review the shipped OptimalX Salla React theme in `C:\Users\Ahmed\OneDrive\Desktop\optimalx` against `DIRECTION.md` (scratchpad C:\Users\Ahmed\AppData\Local\Temp\claude\c--Users-Ahmed-OneDrive-Desktop-optimalx\ff691bcb-344e-4a12-98cc-98d989eacf7d\scratchpad\) and `PLAN-final.md` section 5.3. You verify and judge; you do not edit source.

Method: `pnpm build` then `pnpm preview --port 5360` (stop it when done); walk with the playwright MCP (chrome-devtools if free) in Arabic at 390x844 and 1440x900: `/ar/`, `/ar/kitchen-sink`, PDPs p1996831868 (whey), p1497955814 (food), p1141798217 (bundle), p487045117 (service), `/ar/latest-products`, `/ar/search?q=zzzz`, `/ar/services`, `/ar/branch`, `/ar/about`, `/ar/contact`, `/ar/cart`, `/ar/no-such-page`. If the dev store answers 429 (store settings rate limit), wait five minutes and retry once; if it persists, review the kitchen sink and the compiled CSS plus the component sources and say the walk was blocked. Save screenshots to `scratchpad/g2/<route>-<width>.png`.

Judge, with evidence per route: the D 9.2 to 9.8 rows (structure, contrast, keyboard, forms, media and motion, reflow, RTL) marking each B row block or pass; the anti-template mapping D 9.9 (at least four dimensions visibly present: the wedge bands, the goal-led hierarchy, the spec chips and nutrition table, the graphite and orange discipline, Cairo-only type, the sprite); block heights against D 6.x; one wedge per screen; the A1 to A9 amendments; the motion rows of D 7.1 with reduced motion emulated; the permutations the builders flagged (B3 buy-zone order: SpecChips, SupplyCalculator, DeliveryPromise, then the engine Options and buy row; B1 header sticky not fixed; B2 hero without assets renders the headline on the plate) with an explicit accept or refuse each; the sprite silhouettes (`public/assets/icons/ox-sprite.svg`); the logo stopgap; the bottom tab bar over the bottom edge of full-width controls. Run Lighthouse (accessibility category at least) on `/ar/` and one PDP if the tools allow. Write `scratchpad/G2-report.md` and return the delegation JSON envelope (`severity`, `findings[{file,line,issue,dimension}]`, `recommendations`, `blockers`, plus `accepted_permutations` and `refused_permutations`). No em-dashes.
