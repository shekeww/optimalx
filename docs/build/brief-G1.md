# Brief G1: security review (after wave 1 on B3; final pass before G4)

You are the security reviewer of the mastermind team (OWASP focus). Review the OptimalX Salla React theme in `C:\Users\Ahmed\OneDrive\Desktop\optimalx` against `PLAN-final.md` section 5.2 (scratchpad C:\Users\Ahmed\AppData\Local\Temp\claude\c--Users-Ahmed-OneDrive-Desktop-optimalx\ff691bcb-344e-4a12-98cc-98d989eacf7d\scratchpad\). Scope, in order: `app/components/product/lib/sanitizeHtml.ts` and `tests/security/sanitize.test.ts` (allowlist, attribute stripping, `javascript:` and `data:` hrefs, entity smuggling, U+2028/U+2029, nested and unterminated tags, re-serialisation from the token tree; write and run additional vectors if the corpus is thin); every `dangerouslySetInnerHTML` under `app/` (inventory must be exactly JSON-LD through `toScriptText`, sanitised description and article HTML); URL construction in `app/components/seo/head.ts` and `registerHeadHooks.tsx` (origin only from `store.url`); `wa.me` links (digits only); newsletter and converter inputs; `pnpm audit`; no new dependency versus HEAD `git diff HEAD -- package.json pnpm-lock.yaml`; no secret or `store.settings.keys.*` echoed except the maps key into `SallaMap`. Do not modify files; return the delegation JSON envelope (`severity`, `findings[{file,line,issue,dimension}]`, `recommendations`, `blockers`) and write it to `scratchpad/G1-report.md`.
