# S5 — Store data pipeline

Batch owner: builder S5. Files owned: `scripts/salla-auth.mjs`, `scripts/salla-categories.mjs`,
`scripts/salla-lib.mjs`, `docs/build/store-data-runbook.md`, `docs/build/taxonomy-ids.json` (output,
written only by a live `--apply`), `tests/scripts/{salla-auth,salla-categories}.test.ts`; edits to
`.gitignore`, `docs/build/store-write-log.md` (append, only on a live write), `docs/build/owner-checklist.md`
A.1/A.2 (pointer line).

**Do NOT run `--apply` this session** (explicit instruction). `--plan` must work fully offline. No `git`
write commands.

## Log

- Read, in order: `CONDUCTOR-2026-09-21.md`, `PLAN-ship-2026-09-21.md` (§0, §2 Contract A + Batch S5, §5
  risks 2/3/6), `store-write-log.md` (log format), `research/FINAL-catalogue.md` §A/§B, `salla-ids.json`,
  `research/optimalx-catalogue.csv` (sku, salla_id, categories, brand columns).
- Checked `docs/build/progress/S1.md`: **does not exist yet**. `app/content/taxonomy.json` and
  `locales/partials/tax.{ar,en}.json` do not exist. Per the brief: build against the CSV + FINAL-catalogue
  §A first, switch the reader to the JSON when it lands, CSV stays as the fallback with a printed warning.
  `salla-categories.mjs`'s `loadTaxonomy()`/`loadNames()` already implement that switch — no further action
  needed when S1 lands; a re-run of `--plan` picks up the JSON automatically.
- Verified CSV shape: 47 data rows, `categories` column = `leaf-slug | goal-slug ...` (pipe-separated),
  `brand` column = the Latin brand name. Cross-checked every row's leaf/goal slugs against the conductor §4
  taxonomy: all resolve except one brand gap (below).
- **Finding, reported not silently worked around**: `OX-028` (سنتروم للرجال ملتي فيتامين) carries
  `brand=Centrum`, but Centrum has no row in `FINAL-catalogue.md` §B. Added it to `salla-categories.mjs`'s
  `BRAND_SOURCE` with the transliteration the CSV's own `name_ar` column already uses ("سنتروم") and no
  invented country, with a comment pointing at this gap; flagged again in the runbook's brands step for the
  owner to backfill proper §B research (site, press kit) later. This is a data gap in the research file, not
  a script defect.
- Built `scripts/salla-lib.mjs`: env loading (`process.env` wins over hand-parsed `.env.salla`), a
  rate-limited fetch client (300 ms spacing, 429/5xx exponential backoff, `Cf-Mitigated: challenge`
  detection that throws `SallaChallengeError` with no retry), token-redacting logger, and the
  `store-write-log.md` appender/reader (JSON run block inside an HTML comment so the file stays both
  human-readable and machine-parseable for `--rollback`).
- Built `scripts/salla-auth.mjs`: callback server bound to `127.0.0.1:8787` only, random `state` verified via
  a pure `parseCallback()` (state mismatch rejected before any exchange), `.salla-token.json` written with
  `{access_token, refresh_token, expires_at, scope}`, `--refresh`, `SALLA_ACCESS_TOKEN` override read by
  `salla-lib.mjs`'s `resolveAccessToken()`. Never logs a token (only the fixed strings "received" /
  "written").
- Built `scripts/salla-categories.mjs`: `--plan` is pure (no `fetch` import reachable from that path);
  `--apply [--brands] [--assign] [--images]` idempotent by `metadata_url` then name, parents created before
  children (with a `parent_id`-on-body attempt and a `/categories/{id}/subcategory` fallback per the
  verified-facts brief), read-back diff per write, `--rollback <run-id>` undoes only what that run created
  (skips reused categories/brands) and restores product category/brand arrays from the pre-write read-back.
- Wrote `tests/scripts/salla-auth.test.ts` and `tests/scripts/salla-categories.test.ts` with injected
  `fetch`/`fetchImpl` — no network in any test.
- Wrote `docs/build/store-data-runbook.md`: preflight (Partners scopes + redirect URL, MCP-declined note,
  portal link, install link), local run, Vercel-sandbox run, dashboard alternative, verification, rollback.
- `.gitignore`: added `.salla-token.json`.
- `docs/build/owner-checklist.md` A.1/A.2: added a one-line pointer to the runbook (not marked done — never
  run).

## Verification run (paste actual output below as each step completes)

- `pnpm vitest run tests/scripts` → **2 files, 54 tests, all passed** (salla-auth.test.ts + salla-categories.test.ts). No network call in any test (every `fetch` is injected; file I/O uses `os.tmpdir()` scratch paths, never the real `.env.salla`/`.salla-token.json`/`store-write-log.md`).
- `node scripts/salla-categories.mjs --plan` → exit 0, no warnings, prints `taxonomy source: contract-a` (S1 landed `app/content/taxonomy.json` mid-session; the reader switched to it automatically, exactly as designed — see the S1-landed note below).
- `node scripts/salla-categories.mjs --plan --brands --assign` → exit 0: **25 categories** (10 type roots + 5 protein children + 4 utility + 6 goals, parents-first ordering verified), **22 brands** (FINAL-catalogue.md §B's 21 + the documented `Centrum`/`Myprotein` gap-fills — no unresolved brand warnings), **47 product assignments**, every one with a resolved Salla product id (no `[MISSING Salla id]`).
- `git check-ignore .salla-token.json` → `.salla-token.json` (ignored, confirmed).
- `pnpm typecheck` → 0 errors (`scripts/*.mjs` and `tests/` are outside tsconfig's `include`, unchanged by this batch).
- `pnpm test` → **990/992 passed**. The 2 failures (`tests/i18n-keys.test.ts` missing `ox.tax.*` base keys, `tests/home/OxHero.test.tsx` hero subline content) are in files S5 does not own (`locales/ar.json`/`en.json` base merge is S1/conductor territory; `app/components/home/OxHero.tsx` is S2's) and reflect other batches' in-flight work mid-session, not a regression from this batch — S5 touched no `locales/*` file and no `app/` file.
- `pnpm check:all` → **0 problems** across all five checks (copy, jsonld, rtl, motion, strings).
- `pnpm build` / `pnpm check:all` conductor-level rerun: deferred to the conductor's checkpoint per §6 rule 4 (full build once at the end, shared across builders); nothing in this batch touches build-time surfaces (`scripts/*.mjs` are not bundled).

## S1 landed mid-session

While this batch was in progress, `app/content/taxonomy.json` appeared (S1's Contract A), matching my
CSV-fallback taxonomy's 25 nodes and structure exactly (a good cross-check of both). `locales/partials/
tax.{ar,en}.json` have not landed yet. No action needed on my side: `loadTaxonomy()` already prefers the
JSON when present and `loadNames()` already prefers the tax partials when both exist — both will pick up
S1's remaining output automatically on the next `--plan`/`--apply` run, with no code change.

## Findings for the owner (not silently worked around)

1. **Two brands used by real SKUs are missing from FINAL-catalogue.md §B**: `Centrum` (OX-028) and
   `Myprotein` (OX-039, OX-040). Added to `salla-categories.mjs`'s `BRAND_SOURCE` with the transliteration
   the CSV's own `name_ar` column already uses (`سنتروم`, `ماي بروتين`) and no invented country/facts;
   flagged again in `docs/build/store-data-runbook.md` §3 for the owner to backfill proper §B research.

## Deliverables (file paths)

- `scripts/salla-lib.mjs`, `scripts/salla-auth.mjs`, `scripts/salla-categories.mjs`
- `tests/scripts/salla-auth.test.ts`, `tests/scripts/salla-categories.test.ts`
- `docs/build/store-data-runbook.md`
- `.gitignore` (+ `.salla-token.json`)
- `docs/build/owner-checklist.md` (A.1/A.2 one-line pointers)
- `docs/build/progress/S5.md` (this file)

Not written this session (by design): `docs/build/taxonomy-ids.json` (only written by a live `--apply`),
`docs/build/store-write-log.md` (only appended by a live write — none occurred).
