# Brief B7: content merge and reconciliation (wave 3, one builder)

Execute batch **B7** of the OptimalX Salla React theme in `C:\Users\Ahmed\OneDrive\Desktop\optimalx` per the B7 section of `PLAN-final.md` (scratchpad C:\Users\Ahmed\AppData\Local\Temp\claude\c--Users-Ahmed-OneDrive-Desktop-optimalx\ff691bcb-344e-4a12-98cc-98d989eacf7d\scratchpad\). Read `builder-preamble.md`, PLAN-final sections 2 and B7, `research/FINAL-content.md` sections 1 to 9 (the audited source of every customer-facing string; it was corrected on 2026-09-18: no reply-time promise, no payment method names, `{threshold}` placeholders, collagen 10 to 20 g, biotin 3000 to 10000 mcg), `research/claims-audit.md`, `research/FINAL-claims-source.md`.

Steps: (1) `pnpm i18n:merge` then confirm `pnpm i18n:merge -- --check` reports nothing pending; (2) convert every remaining single-brace placeholder in `locales/ar.json` and `locales/en.json` to i18next double braces; (3) reconcile every `ox.*` value against FINAL-content 9 and sections 1 to 6 (the spec wording wins over builder placeholders; keep builder keys that are used in `app/` even if FINAL-content lacks them, and list them in `docs/content/gaps.md`); (4) replace every `TODO-copy:` value with the FINAL-content wording where it exists, otherwise a neutral MSA line that makes no claim, and list it in `gaps.md` with the key; (5) write `tests/i18n-usage.test.ts` (every `t('ox.` key used under `app/` exists in both locale files, and both files have identical sorted key sets) and simplify `tests/i18n.test.ts` (drop the partial rules); (6) write `docs/content/guides/*.html` for the three full guides in FINAL-content 7 (HTML that passes the B3 `sanitizeHtml` unchanged, with a test) and `docs/content/policies/*.md` from FINAL-content 6.3; (7) delete `locales/partials/` and the partial fixtures the tests no longer need; (8) `pnpm check:copy` 0 problems and 0 unlisted TODO-copy, `pnpm test` green, `pnpm build` green. Claims gates of PLAN-final 5.1 are binding: no literal threshold, no payment names, no reply promise, no distributor claim, no popularity, no health outcome language, no professional titles, no dialect, no diacritics, no em-dashes. Write files with the Write tool; keep `scratchpad/B7-progress.md`; no git index changes; return only the delegation JSON envelope.

## Addendum (2026-09-19)
- The PDP FAQ price item is mandatory (DIRECTION 5.4) but `ox.content.faq.price_a` and `pdp_allergens_a` are TODO-copy. Write both in MSA with no claim: the price answer says the final price is shown on the product page with the servings and serving size so the shopper can compare per serving themselves (no VAT-inclusive statement, that line is gated on `vat_number`); the allergens answer says allergens are listed under "تنبيه" on every product and the shopper should read the label.
- Glossary: `app/content/glossary.ts` (Foundation-2's, you may modify it in wave 3) covers none of the label macronutrients. Add plain-Arabic one-line explanations (and English twins) for protein, carbohydrates, fat, saturated fat, sugars, fibre, sodium, calories, caffeine, zinc, vitamin C, magnesium, so the nutrition table's third column is filled on most products; nutrition-level statements only, no outcomes.
- Keep every `ox.trust.*` value identical between b2 and b3 partials (trimmed); if the merge names a conflict, the FINAL-content section 9 value wins.
