# Handoff, 2026-09-21 22:45 Cairo

For whoever continues this (any account): read this file, then `docs/build/CONDUCTOR-2026-09-21.md`, `docs/build/PLAN-ship-2026-09-21.md`, and `docs/build/progress/*.md`. Then `git log --oneline -10` because the owner's tooling commits between sessions.

## Where the code is

- One branch: **`main`** on https://github.com/shekeww/optimalx (remotes `origin` and `optimalx_remote` point at the same repo). The work branch and `master` were merged into `main` and deleted tonight; no history was rewritten. Never commit `.playwright-mcp/` or `.offline-preview.log` (now ignored).
- HEAD `380b2cf` is green: `pnpm typecheck` 0, `pnpm test` 1006/1006, `pnpm check:all` five checks 0, `node scripts/check-claims.mjs` 0, `pnpm build` green.
- Theme framework: React (`@salla.sa/twilight-theme-engine`). There is no Twig in this repo; any brief that says "preserve Twig" was written for a different project.

## Getting the build onto the store (owner clicks, in this order)

1. Partners portal, theme **1938498306** ("OptimalX", React, status development, repo not connected yet): connect the GitHub repository `shekeww/optimalx`, branch `main` (`docs/DEPLOY.md` step 1; the theme id printed there, 10165682, is stale: use 1938498306).
2. Installation method: private (`docs/DEPLOY.md` step 2), submit for review (step 3), install on store 1888890798 and set the version live (step 4), fill the gated settings (step 5). Until review passes, the live domain keeps the Raed theme with the injected skin (app 755989931 snippets), which is what you see now.
3. Store data (categories, brands, product assignment): `docs/build/store-data-runbook.md`. Preflight is yours: on https://portal.salla.partners/apps/755989931 add scopes categories/brands/products read_write and redirect URL `http://localhost:8787/callback`, install the app on the store from https://s.salla.sa/apps/install/755989931, then `node scripts/salla-auth.mjs`, `node scripts/salla-categories.mjs --plan --brands --assign --images`, then the same with `--apply`. Dashboard alternative in the runbook. Until this runs, category and goal links fall back to search links by design.
4. Maintenance mode off last.

## Program status (mastermind ship program)

| Batch | Status | Next |
|---|---|---|
| S1 Taxonomy in theme | Steps 1-4 done (contracts, `useTaxonomyLinks`, nav, header, drawer; header trigger fix landed by the conductor) | Steps 5-7: `ListingPage` h1/intro/chips from `nodeBySlug`, `/categories` route (`app/routes/categories.tsx` does not exist yet: `OxCategories` "عرض الكل" still links to it), fixture overlay `OFFLINE_TAXONOMY=1` |
| S2 Home sections | Not started | Needs the designer's direction (never produced: the agent died twice on limits). Owner imagery `docs/design-target/Firefly_gpt-image_on category cards...png` is the direction of record |
| S3 SEO/AEO/GEO | Not started (died at reading stage twice) | Plan §2 Batch S3, all seven steps; titles from `nodeBySlug(...).titleKey` |
| S4 Content persona | Done (`docs/content/changes-2026-09-21.md`, `scripts/check-claims.mjs`, 19 keys) | S7 wires `check-claims` into `check:all` |
| S5 Store pipeline | Done, never applied | Owner preflight above |
| S6 Pages | Only the kitchen-sink route gate landed (`app/routes/kitchen-sink.tsx`; its chunks still ship in `dist/`, cosmetic) | Plan §2 Batch S6 steps 2-6 |
| G2 designer | Not produced | Brief in the conversation is reproducible from plan §2 S2 "Data" and the conductor file §2 |
| S7 Deployment | Gates green; branch consolidated; theme id corrected here | `docs/DEPLOY.md` numbers and theme id need updating; `twilight.json` `free_shipping_threshold` default still "299" (plan says empty until the shipping rule exists) |

Run at most three builders at once: seven in parallel emptied a session window in ten minutes, three emptied one in about an hour.

## Audit of the "DEPLOY CURRENT OPTIMALX BUILD TO SALLA" brief

Verdict: usable after five corrections; its premise about the architecture is wrong.

| Claim in the brief | Verified state | Correction |
|---|---|---|
| "already a Salla Twilight theme ... preserve Twig/Twilight, do NOT convert to React/Vite" | The repo is a React/Vite Twilight theme (engine 1.0.47, TanStack Router); no Twig files exist | Drop the Twig clauses; the deployable unit is this React theme via Partners |
| Primary branch `main`, remote `optimalx_remote` | Both exist now; `main` is the only branch and both remotes are the same repo | `git pull optimalx_remote main` works as written |
| Theme id 1938498306 | Correct (Partners lists exactly one theme, React, development, repo not connected) | `docs/DEPLOY.md` and `docs/build/STATUS.md` still say 10165682: stale |
| "Store shows an outdated version of the design" | The store runs Raed plus the skin from app 755989931; the React theme was never installed (needs Partners connection + review) | The fix is connection + review + install, not a push |
| "Do NOT merge docs/engine-defect-and-spec-trueup into main" | All build work lived on that branch; `main` held only 389 screenshots and a log | Merged tonight; `main` now carries the build |
| Do not commit preview artefacts | They were committed on `main` today by tooling | Untracked and ignored in `380b2cf` |
| Preserve English LTR, localization | Store is single-language (`is_multilingual: false`); `/en` is not served until English is enabled in the dashboard | Not a regression; documented in `owner-checklist.md` item 11 |

## Verification commands

`pnpm typecheck && pnpm test && pnpm check:all && node scripts/check-claims.mjs && pnpm build`. Preview: `pnpm preview:offline`, then every URL with `?storeId=1888890798` (`docs/build/offline-preview.md` explains why).
