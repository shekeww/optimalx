// @auto-generated
import { createFileRoute } from '@tanstack/react-router';
import { LogoutRedirect } from '@salla.sa/twilight-theme-engine/routes/seo-redirects';

export const Route = createFileRoute('/{-$locale}/logout')({
  component: LogoutRedirect,
});
