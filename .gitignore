node_modules/
dist/
.wrangler/
.env
.env.*
.DS_Store
stats.html
coverage/
tsconfig.tsbuildinfo

# Local-only agent instructions — not shared
CLAUDE.md

# A second Salla CLI scaffold was generated here by mistake; it is not part of the theme.
optimalx/

# TanStack build cache
.tanstack/

# Generated by the Twilight vite plugin on every dev and build run. It bakes the
# absolute pnpm path of the engine on the machine that generated it, so a
# committed copy carries one machine into everyone else's build.
app/routeTree.gen.ts

# Browser-automation session artifacts (console logs, screenshots).
.playwright-mcp/

# Combined log of pnpm preview:offline (scripts/preview-offline.mjs)
.offline-preview.log

# OAuth token pair written by scripts/salla-auth.mjs. Never committed.
.salla-token.json

# Preview and browser-session artefacts (never shipped, never reviewed)
.playwright-mcp/
.offline-preview.log

# Reference imagery and mockups stay local; 83 files (128 MB) were committed by the owner tooling on 2026-09-22 and removed from the index
references/

# Build-time Vite cache (vite.config.ts cacheDir override)
.vite-build/

# a Shopify CLI app scaffold the owner created on 2026-09-24, its own git repo, never part of the theme
optimal-x-seed/

# probe output that browser tools write into the repo root during agent verification
/s1[0-9]*-*.json
/s1[0-9]*rv-*.json
/.playwright-mcp/
